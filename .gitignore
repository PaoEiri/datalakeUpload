# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/
*.egg

# Environment
.env
infra/.env

# Data
data/raw/
data/processed/

# Dataset de referencia del TFM (ficheros fuente reales) — se queda en local,
# no se sube a GitHub. scripts/load_tfm_dataset.py y la API los necesitan en
# disco, pero no forman parte del repo.
dataset/

# Notas/documentos de trabajo internos — se quedan en local
consideraciones/

# Notebooks checkpoints
.ipynb_checkpoints/

# MLflow local runs (when not using remote server)
mlruns/
mlartifacts/

# OS
.DS_Store
.guion.md

# dbt build artifacts (generados por `dbt run`/`dbt docs generate`,
# no son fuente — se regeneran siempre localmente)
dbt/target/
dbt/logs/
/manifest.json
/catalog.json
